window.onload=function () {    var otab = document.getElementById('tab')//获取按钮    var odiv=document.getElementById('f1-right');//获取内容区div    var btn=otab.getElementsByTagName('li');//获取div下的input    var div2=document.getElementsByClassName('div2') ;//获取div(内容区)下的div    for(var i=0;i<btn.length;i++)//循环每个按钮    {        btn[i].index=i//btn[i]是指定button的第i个按钮;为该按钮添加一个index属性，并将index的值设置为i        btn[i].onmouseover=function()//按钮的第i个点击事件        {            for(var i=0;i<btn.length;i++)//循环去掉button的样式，把div隐藏            {                btn[i].className='' //清空按钮的样式                div2[i].style.display='none'//隐藏div            }            this.className='active'//自身添加active            div2[this.index].style.display='block'//this.index是当前div        }    }}//一楼小轮播//left获取function getcss(obj,name) {    if (obj.currentStyle){        return obj.currentStyle[name]    } else {        return getComputedStyle(obj)[name];    }}﻿var obox = document.getElementById("swiper");var oul = document.getElementById("swiper-list");var left = getcss(oul,"left");left = left.replace("px","");var timer = setInterval(function () {    left -= 389;    oul.style.left = left + "px";    if (left == -1556){        oul.style.left = -389 + "px";        left = -389;    }},1000)